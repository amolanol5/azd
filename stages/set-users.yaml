parameters:
  - name: test
    type: string
    default: test

stages:
  - stage: stage
    displayName: stage
    jobs:
      - job: job
        displayName: job
        steps:
          - script: |
              ls

